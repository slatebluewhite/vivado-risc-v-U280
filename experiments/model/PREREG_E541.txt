E541 사전등록 — E540 수정(config_ex 정지 가드) 후 전체 재검증

[1] 32x32 ms8 격자 8칸 전부 PASS (wedge 포함 소멸).
[2] 32x32 ms4 (2,2,0)/(2,1,0) PASS.
[3] 16x16 ms8 4칸, 16x16 ms4, 8x8 ms4 격자 PASS 유지.
[4] **전환 상수는 변한다**: 모든 phase 경계에 drain+4사이클 정지가 들어가므로
    c(from) 은 0~3 이 아니라 drain 꼬리+5 규모(대략 +5~+40)로 커지고, 여전히
    from-shape 만의 함수(같은 문맥·대각 포함)여야 한다. 새 표를 기록한다.
[5] 사이클 모형의 phase 내부(첫 phase 절대값)는 변하지 않는다 — 가드는 경계에만 작용.
