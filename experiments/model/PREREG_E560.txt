E560 사전등록 — SeqShapes 진짜-acc(accModeOv=2) 판정

[1] (2,2,0) M=1 K=128 N=128 sb4 rd4 accMode=2: **shape2 phase 실패** (WAW 드롭 —
    M=1 의 같은 acc 행 재방문 간격이 tr=4 로 경계). fake acc(같은 셀 am=0)는 PASS
    유지 — 델타가 곧 판정.
[2] 대조: (4,128,128)류 M=4 (재방문 여전히 tr=4 지만 M=4 행이 순환해 같은 행 간격
    4*…)? — M=4 는 같은 행 재방문이 tr*1=4 가 아니라 여러 행 순환으로 완화 —
    예측 유보(관찰만). (2,2,0) M=1 K=64 N=64 am=2 도 실패 예측(같은 기전).
[3] 재현 시 수정: E516 skid queue 를 floor 4 에도 상시 인스턴스 → am=2 재검증 PASS,
    fake-acc 전 격자 회귀 무결 → 비트스트림 #5.
